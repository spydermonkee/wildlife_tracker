<h1> Edit <%= @one_species.name %> </h1>

<form action="/species/<%= @one_species.id %>/edit" method="post" class="form-inline" >
  <label for="species_name">Species Name</label>
  <input id="species_name" name="species[name]" type="text" value="<%= @one_species.name %>"></input>
  <button class="btn btn-warning">Edit Species</button>
</form>
